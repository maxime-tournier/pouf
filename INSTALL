Installation
============

Just copy the directory inside either sofa/applications/plugins or
sofa/applications-dev/plugins and re-run cmake.

Edit CMakeCache.txt to set:

SOFA-DEVPLUGIN_POUF:BOOL=ON

Save, and rebuild.

That should be it.

